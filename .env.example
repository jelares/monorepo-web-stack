# Project Configuration
APP_NAME=myapp

# AWS Configuration (see README for setup guide)
AWS_PROFILE=my-profile-name

# Optional: Override region (defaults to profile's region)
# AWS_REGION=us-east-1

# Default Lambda Runtimes
NODEJS_RUNTIME=nodejs20.x
PYTHON_RUNTIME=python3.12

# Versioning (staging/prod only - dev has no versioning)
VERSIONS_TO_KEEP=5